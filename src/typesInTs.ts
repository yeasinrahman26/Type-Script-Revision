// ype Inference vs. Annotation

// 1. Type Inference

// TypeScript automatically figures out the type based on the assigned value. You don't need to explicitly specify the type.

{
  /**2. Type Annotation

Type annotation means you explicitly tell TypeScript what the type should be. */
}

let drink = "Coca_cola";

let cups = Math.random() > 0.5 ? 10 : 2;

let channelName = "ChaiAurCode";

channelName = "123";

let chaiFlavour: string = "dudh Chai";
chaiFlavour = "masala Chai";

let chaiOder: boolean = true;

chaiOder = false;
